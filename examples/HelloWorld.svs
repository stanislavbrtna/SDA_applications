function init {
  scr = sys pAddScreen();
  sys pSetMainScr(scr);

  sys pAddText(1, 1, 8, 2, "Hello, world!", scr);

  ok = sys pAddButton(1, 3, 3, 4, "Ok", scr);
}

function update {
  if (sys pGetEventC(ok) == EV_RELEASED) {
    sys exit();
  }
}
